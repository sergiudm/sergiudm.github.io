<?xml version="1.0" encoding="utf-8" standalone="yes"?><rss version="2.0" xmlns:atom="http://www.w3.org/2005/Atom"><channel><title>RNN on Qijun's Blog</title><link>https://sergiudm.github.io/tags/rnn/</link><description>Recent content in RNN on Qijun's Blog</description><generator>Hugo -- gohugo.io</generator><language>en-us</language><lastBuildDate>Wed, 09 Apr 2025 21:00:00 +0000</lastBuildDate><atom:link href="https://sergiudm.github.io/tags/rnn/index.xml" rel="self" type="application/rss+xml"/><item><title>The Evolution of RWKV (Part 1)</title><link>https://sergiudm.github.io/p/the-evolution-of-rwkv-part-1/</link><pubDate>Wed, 09 Apr 2025 21:00:00 +0000</pubDate><guid>https://sergiudm.github.io/p/the-evolution-of-rwkv-part-1/</guid><description>&lt;img src="https://sergiudm.github.io/p/the-evolution-of-rwkv-part-1/imagecopy.png" alt="Featured image of post The Evolution of RWKV (Part 1)" /&gt;&lt;h1 id="overview"&gt;Overview
&lt;/h1&gt;&lt;p&gt;Welcome to this series exploring the RWKV architecture. RWKV is a novel neural network design that merges the efficient inference of Recurrent Neural Networks (RNNs) with the parallelizable training of linear attention mechanisms.&lt;/p&gt;
&lt;p&gt;In this series, we will explore the design and implementation of RWKV. Along the journey, we will also discuss how it relates to other emerging architectures, such as &lt;a class="link" href="https://arxiv.org/abs/2312.06635" target="_blank" rel="noopener"
&gt;GLA&lt;/a&gt; and &lt;a class="link" href="https://arxiv.org/abs/2406.06484" target="_blank" rel="noopener"
&gt;DeltaNet&lt;/a&gt;.&lt;/p&gt;
&lt;h1 id="prerequisite-traditional-softmax-attention"&gt;Prerequisite: Traditional Softmax Attention
&lt;/h1&gt;&lt;p&gt;Before diving into RWKV, we must first review the traditional softmax attention mechanism—the backbone of modern Transformers.&lt;/p&gt;
&lt;p&gt;The attention mechanism enables a model to focus on specific parts of an input sequence when making predictions. It computes a weighted sum of input tokens, where the weights are derived from the similarity between a &lt;em&gt;query&lt;/em&gt; vector and a set of &lt;em&gt;key&lt;/em&gt; vectors.&lt;/p&gt;
&lt;p&gt;Given a sequence of $n$ tokens $\mathbf{x} = \begin{bmatrix} \mathbf{x}_1 \ \mathbf{x}_2 \ \vdots \ \mathbf{x}_n \end{bmatrix} \in \mathbb{R}^{n \times d}$, the (non-causal) attention output can be expressed as:&lt;/p&gt;
$$
\text{Attn}(t) = \sum_{i=1}^n \alpha(i,t)\mathbf{v}_i=\sum_{i=1}^{n} \frac{\text{sim}(\mathbf{q}_t, \mathbf{k}_i)}{\sum_{j=1}^{n} \text{sim}(\mathbf{q}_t, \mathbf{k}_j)} \mathbf{v}_i=\sum_{i=1}^{n} \frac{\exp(\mathbf{q}_t \mathbf{k}_i^T)}{\sum_{j=1}^{n} \exp(\mathbf{q}_t \mathbf{k}_j^T)} \mathbf{v}_i \in \mathbb{R}^{d}
$$&lt;p&gt;Here, $\mathbf{q}_t$, $\mathbf{k}_i$, and $\mathbf{v}_i$ represent the query, key, and value vectors for the $t$-th and $i$-th tokens, respectively. The attention weights $\alpha(i,t)$ are computed using the softmax function, which normalizes the scores to ensure they sum to 1.&lt;/p&gt;
&lt;h2 id="computation-cost"&gt;Computation Cost
&lt;/h2&gt;&lt;p&gt;It is easy to see that attention computation has a time complexity of $O(n^2)$. For every token $t$, we must compute attention weights against &lt;strong&gt;all&lt;/strong&gt; $n$ tokens.&lt;/p&gt;
&lt;p&gt;This quadratic scaling becomes a significant bottleneck for long sequences. Even during inference with KV caching enabled, the cost of generating a full sequence remains quadratic regarding the sequence length, as the history the model must attend to grows larger with every step.&lt;/p&gt;
&lt;h1 id="rnns"&gt;RNNs
&lt;/h1&gt;&lt;p&gt;Unlike the attention mechanism, Recurrent Neural Networks (RNNs) process sequences &lt;strong&gt;one token at a time&lt;/strong&gt;, maintaining a hidden state that updates at each time step.&lt;/p&gt;
&lt;p&gt;The hidden state is derived from the previous hidden state and the current input token. The formulation is as follows:&lt;/p&gt;
$$
\mathbf{h}_t = f(\mathbf{h}_{t-1} \mathbf{W}_{h} + \mathbf{x}_t \mathbf{W}_x+ \mathbf{b}_h)
$$$$
\mathbf{y}_t = g(\mathbf{h}_t \mathbf{W}_y + \mathbf{b}_y)
$$&lt;p&gt;Where $\mathbf{h}_t$ is the hidden state at time step $t$, and $\mathbf{y}_t$ is the output. The functions $f$ and $g$ are typically non-linear activations. $\mathbf{W}_h$, $\mathbf{W}_x$, and $\mathbf{W}_y$ are the weight matrices for the hidden state, input, and output, respectively, while $\mathbf{b}_h$ and $\mathbf{b}_y$ are the bias vectors.&lt;/p&gt;
&lt;h2 id="computation-cost-1"&gt;Computation Cost
&lt;/h2&gt;&lt;p&gt;Notice that the hidden state $\mathbf{h}&lt;em&gt;t$ depends only on the &lt;strong&gt;immediate predecessor&lt;/strong&gt; $\mathbf{h}&lt;/em&gt;{t-1}$ and the current input $\mathbf{x}_t$, rather than the entire history ${\mathbf{h}_0, \mathbf{h}&lt;em&gt;1, \ldots, \mathbf{h}&lt;/em&gt;{t-1}}$.&lt;/p&gt;
&lt;p&gt;This means that at each time step, the computation cost is $O(1)$ (ignoring the fixed dimensions of the hidden state). Consequently, the total computation cost for processing a sequence of length $n$ is $O(n)$—linear scaling—which is far more efficient than attention mechanisms for long sequences.&lt;/p&gt;
&lt;h1 id="question-does-on-mean-rnns-are-superior"&gt;Question: Does $O(n)$ mean RNNs are superior?
&lt;/h1&gt;&lt;p&gt;The short answer is &lt;strong&gt;NO&lt;/strong&gt;. While efficient, traditional RNNs historically failed to replace Transformers. There are two primary reasons why RNNs struggle against softmax attention:&lt;/p&gt;
&lt;h2 id="1-parallelization"&gt;1. Parallelization
&lt;/h2&gt;&lt;blockquote&gt;
&lt;p&gt;Although attention has a higher total computation cost, it can be &lt;strong&gt;parallelized&lt;/strong&gt; across all tokens in the sequence.&lt;/p&gt;
&lt;/blockquote&gt;
&lt;p&gt;Let&amp;rsquo;s look at the attention equation again:&lt;/p&gt;
$$
\text{Attn}(t)=\sum_{i=1}^{n} \frac{\exp(\mathbf{q}_t \mathbf{k}_i^T)}{\sum_{j=1}^{n} \exp(\mathbf{q}_t \mathbf{k}_j^T)} \mathbf{v}_i = \text{softmax}(\mathbf{q}_t \mathbf{K}^T)\mathbf{V}
$$&lt;p&gt;In this equation, calculating $\text{Attn}(t)$ does not rely on the output of $\text{Attn}(t-1)$. There are no sequential dependencies on previous time steps during calculation. This allows us to compute the attention output for every token in the sequence simultaneously, offering a massive advantage when training on modern hardware like GPUs or TPUs.&lt;/p&gt;
&lt;p&gt;In contrast, RNNs are inherently sequential. To compute the state for token $t$, you must first finish the computation for token $t-1$. This sequential dependency prevents parallel training, leading to significantly longer training times.&lt;/p&gt;
&lt;h2 id="2-long-range-dependencies"&gt;2. Long-range Dependencies
&lt;/h2&gt;&lt;blockquote&gt;
&lt;p&gt;RNNs struggle to capture long-range dependencies, particularly in very long sequences.&lt;/p&gt;
&lt;/blockquote&gt;
&lt;p&gt;Because the hidden state $\mathbf{h}_t$ is fixed in size and updated at every step, information from early tokens tends to &amp;ldquo;vanish&amp;rdquo; or become diluted as it propagates through the network. Softmax attention, however, has direct access to the entire history (the &amp;ldquo;global view&amp;rdquo;), allowing it to recall information from any point in the sequence with equal ease.&lt;/p&gt;
&lt;h1 id="summary"&gt;Summary
&lt;/h1&gt;&lt;p&gt;In summary, while RNNs offer lower inference costs ($O(n)$ vs $O(n^2)$), traditional RNNs cannot match the training parallelization or the long-range recall capabilities of Transformers.&lt;/p&gt;
&lt;p&gt;In the next post, we will explore how the RWKV architecture solves this dilemma, combining the parallel training of Transformers with the efficient inference of RNNs.&lt;/p&gt;
&lt;h1 id="references"&gt;References
&lt;/h1&gt;&lt;ul&gt;
&lt;li&gt;&lt;a class="link" href="https://arxiv.org/abs/2312.06635" target="_blank" rel="noopener"
&gt;Gated Linear Attention Transformers with Hardware-Efficient Training&lt;/a&gt;&lt;/li&gt;
&lt;li&gt;&lt;a class="link" href="https://arxiv.org/abs/2406.06484" target="_blank" rel="noopener"
&gt;Parallelizing Linear Transformers with the Delta Rule over Sequence Length&lt;/a&gt;&lt;/li&gt;
&lt;/ul&gt;</description></item></channel></rss>